# Local scratch output. NOTE: dist/ is intentionally NOT ignored — CI commits
# it back to the repo so Cloudflare Pages / Gitee Pages can serve it.
out*/
b_native/
b_bgpdump/
*.mrt
mrt_cache/

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/
